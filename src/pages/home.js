import React, { useEffect, useRef, useState } from "react";
import { Container, Row, Col, Dropdown, NavDropdown, Card, Modal, Tooltip, OverlayTrigger, Button, Badge } from "react-bootstrap";

const Home= () =>{


    return(
        <Container>
        <div><h1>HELLO</h1></div>
        </Container>
    )
}
export default Home;
